import React from 'react';
import { NativeBaseProvider, Text } from 'native-base';

export default function App() {
	return (
		<NativeBaseProvider>
			<Text>Car Rental TN</Text>
		</NativeBaseProvider>
	);
}